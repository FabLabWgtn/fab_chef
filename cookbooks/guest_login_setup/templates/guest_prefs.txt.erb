
sudo usermod -aG fabshare $USER
sudo usermod -aG dialout $USER
sudo usermod -aG fabmodules $USER
sudo ifconfig eno1 dow
sudo ifconfig wlp5s0 up
sudo nmcli device wifi connect MUGuests iface wls6 --timeout 3
chown -R $USER: $HOME
#something fancy about if roles to only add the right bits of this
